//V = [VetorVazio]
count = 0
head = V[0]
tail = v[-1]


//teste queue-overflow (estouro de fila cheia)
IsFull(count)
    se count = |V|
        retorna VERDADEIRO
    senão
        retorna FALSO


//teste queue-underflow (estouro de fila vazia)
IsEmpty(count)
    se count = 0
        retorna VERDADEIRO
    senão
        retorna FALSO


//Inserção-Enqueue (Enfileirar)
Enqueue(V,x)
    se IsFull(count) == FALSO //se a fila de V não estiver cheia
        V [tail] ← x
        tail ← tail + 1       
    senão
        erro overflow //erro overflow (de fila cheia)


//Inserção-Enqueue (Enfileirar)
Dequeue (V)
    se ! IsEmpty () //se !não IsEnpty (se o começo(head) não for igual ao final(tail) da fila, ou seja, se a fila não estiver vazia)
        x ← V[head] //x = o primeiro valor da fila
        head ← head + 1 //vai para o próximo valor da fila(o que ficou para trás foi deletado)
        retorna x
    senão
        erro underflow //erro underflow (de fila vazia)
